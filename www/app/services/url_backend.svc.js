(function() {
	'use strict';

    angular.module('chasqui').factory('URL_BACKEND', URL_BACKEND);
    
    function URL_BACKEND() {
        
        var url_backend = ""; // completar con 
        
        return url_backend; 
    }
            
})();